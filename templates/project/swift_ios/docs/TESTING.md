# Testing

Required checks: `xcodebuild -list`, `xcodebuild build`, `xcodebuild test`, `swift test` if Package.swift exists, simulator smoke, accessibility. No pass claim without telemetry. Update NEXT_CODEX_PROMPT.

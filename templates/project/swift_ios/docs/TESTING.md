# Testing

Required checks: `xcodebuild -list`, `xcodebuild build`, `xcodebuild test`, `swift test` if Package.swift exists, simulator smoke, accessibility. No pass claim without telemetry. Record unresolved work only when present; otherwise NEXT_ACTION=NONE.
